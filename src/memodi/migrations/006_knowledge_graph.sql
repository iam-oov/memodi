-- AGE graph creation is handled by ensure_graph() in Python
-- because it requires LOAD 'age' which can't run in a plain SQL migration.
-- This migration is a placeholder to keep the numbering consistent.
SELECT 1;
